# Kanban Board (Vite + React)
Projet minimal pour le Lab7 - Kanban Board (Trello like).

## Usage
- `npm install`
- `npm run dev` -> ouvrir l'URL donnée
- `npm run test` -> exécuter les tests

Contenu: composants TaskForm, Column, TaskCard, tests Jest.
